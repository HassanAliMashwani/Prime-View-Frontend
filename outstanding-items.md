# Outstanding items (not this closeout)
Do not implement these unless a later prompt says so.
- Empty-role RLS (`app.current_role = ''`) — login and public verify
still depend on it. Do not remove in a drive-by.
- Login lockout countdown — API only says “try again later.”
- Password rotation — host/ops, not application code.
- Already shipped (do not redo): inventory JWT + contrast;
Elite 120 allotted to Mashwani with 25,000,000 plot_one_time;
white plot number on allotted (black) map fills.
OI-12 and OI-13 are handled in the OI closeout, not listed here.
